{
  "name": "project-hub-backend",
  "version": "1.0.0",
  "description": "API to dynamically list project files from the /show directory.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "update": "node -r dotenv/config scripts/update-projects.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}
